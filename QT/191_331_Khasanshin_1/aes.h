#ifndef AES_H
#define AES_H
#include <QObject>
#include <QByteArray>



class AES : public QObject
{
//    Q_OBJECT
//public:
//    explicit AES(QObject *parent = nullptr);
////    QByteArray encrypt(QByteArray plainText, QByteArray key);


////     QByteArray decrypt(QByteArray encodedText, QByteArray key);


//public slots:
//   bool do_crypt(QString text, QString key);
//    QVariant encrypt(QString plainText, QString key);


//    QVariant decrypt(QByteArray encodedText, QByteArray key);

};


//class aes
//{
//public:
//    aes();
//};

#endif // AES_H
